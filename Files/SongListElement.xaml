<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TagEditor.Files.SongListElement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TagEditor.Files"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Height="100" CornerRadius="10"  Margin="10,0,10,10">
        <Grid ColumnDefinitions="*,*" Background="#262626">
            <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top">
                <Border Width="30" Height="30" CornerRadius="10">
                    <Image x:Name="SongArtList" Width="50" Height="50" Source="{Binding AlbumArtSource}" ></Image>
                </Border>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top">
                <TextBlock x:Name="SongNameList" Text="{Binding SongName}" Width="100" />
                <TextBlock x:Name="ArtistNameList" Text="{Binding ArtistName}" Width="100"/>
                <TextBlock x:Name="FilePathList" Text="{Binding FilePath}" Width="100"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
